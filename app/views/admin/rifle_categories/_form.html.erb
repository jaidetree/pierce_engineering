<%= form_for(admin_rifle_categories_path) do |f| %>
  <% if @admin_product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_product_category.errors.count, "error") %> prohibited this admin_product_category from being saved:</h2>

      <ul>
      <% @admin_product_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :slug %><br />
    <%= f.text_field :slug %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <%= f.hidden_field :cat_type %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
