<h1>Listing Pages</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Slug</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |admin_page| %>
  <tr>
    <td><%= admin_page.name %></td>
    <td><%= admin_page.slug %></td>
    <td><%= link_to 'Show', page_path( admin_page ) %></td>
    <td><%= link_to 'Edit', edit_admin_page_path(admin_page) %></td>
    <td><%= link_to 'Destroy', admin_page_path( admin_page ), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_admin_page_path %>
